import {View, Text} from 'react-native';
import React from 'react';
import BottomNavigator from '../../components/BottomNavigation/BottomNavigation';

const Main = () => {
  return (
    <View style={{flex: 1,}}>
      <BottomNavigator /> 
    </View>
  );
};

export default Main;